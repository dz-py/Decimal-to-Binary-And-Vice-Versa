decimal = int(input("Input your decimal: "))
list = [decimal]
while decimal > 1:
    decimal //= 2
    list.append(decimal)
binary = []
for element in list:
    if element % 2 == 0:
        binary.append(str(0))
    else:
        binary.append(str(1))
print("".join(binary[::-1]))
